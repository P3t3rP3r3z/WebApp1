<%= form_with(model: section, local: true) do |form| %>
  <% if section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :semster %>
    <%= form.text_field :semster, id: :section_semster %>
  </div>

  <div class="field">
    <%= form.label :section %>
    <%= form.number_field :section, id: :section_section %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.text_field :course_id, id: :section_course_id %>
  </div>

  <div class="field">
    <%= form.label :room_number %>
    <%= form.number_field :room_number, id: :section_room_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
